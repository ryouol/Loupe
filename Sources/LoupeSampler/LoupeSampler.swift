import LoupeCore

/// Telemetry sources. The `TelemetrySource` protocol and its live and replay
/// implementations arrive in M0.6 / M1.1; this is a placeholder.
public enum SamplerPlaceholder {
    /// Default sampling cadence, in hertz.
    public static let defaultCadenceHz = 10
}
